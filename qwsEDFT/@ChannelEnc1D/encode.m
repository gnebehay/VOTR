%ChannelEnc1D/encode
%
%  ch = encode(obj, vals)
%    Channel encodes the values in 'vals' (row vector) using the encoding 
%    specified in the object. Each column in 'ch' is one encoded value from
%    'vals'.
%
%  ch = encode(obj, vals, n)
%    Returns only the value of the n'th channel
%
